## 1. Document os_disk_size_gb in README

- [ ] 1.1 Add `os_disk_size_gb` row to the variable reference table (near `build_vm_size` and `build_timeout_minutes`)
- [ ] 1.2 Add guidance note about when to increase disk size (large Windows Update cache, additional software installs)
